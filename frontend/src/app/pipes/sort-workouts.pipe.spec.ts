import { SortWorkoutsPipe } from './sort-workouts.pipe';

describe('SortWorkoutsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortWorkoutsPipe();
    expect(pipe).toBeTruthy();
  });
});
