import { SortExercisesPipe } from './sort-exercises.pipe';

describe('SortExercisesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortExercisesPipe();
    expect(pipe).toBeTruthy();
  });
});
